import { PageStub } from "@/components/page-stub";

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      description="Profile, workspace, and account settings."
      issue="Issue #2 (Authentication & Workspaces) / Issue #5 (Product Experience)"
    />
  );
}
