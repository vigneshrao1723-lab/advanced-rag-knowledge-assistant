import { PageStub } from "@/components/page-stub";

export default function LoginPage() {
  return (
    <PageStub
      title="Log in"
      description="Registration, login, and session management."
      issue="Issue #2 (Authentication & Workspaces)"
    />
  );
}
