import { PageStub } from "@/components/page-stub";

export default function WorkspacePage() {
  return (
    <PageStub
      title="Workspace"
      description="Workspace settings, membership, and roles."
      issue="Issue #2 (Authentication & Workspaces)"
    />
  );
}
